import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
 
    <App/>

);

//Тигран,что-то я замудрила и запуталась,кнопки удаления не работают,выбивает ошибка removeTask is not a functionбхотя вроде правильно написано,не понимаю
// https://yuliiashulimenko.github.io/React-ToDoList/